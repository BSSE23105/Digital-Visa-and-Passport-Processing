# Ensure you have MongoDB running on your machine (default port 27017).
# The database will be created automatically when you first write to it.
PORT=5000
DB_URI=mongodb://localhost:27017/passportVisaDb